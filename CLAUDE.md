# CLAUDE.md

這份檔案是給 Claude Code 看的專案規則，每次工作前會自動讀取。
專案：test-repo（Git / GitHub 練習用專案）

---

## 分支規則（最重要）

1. **絕對不要直接在 `main` 分支上修改檔案。**
   - 動工前，先確認目前站在哪個分支（`git branch` 或 GitHub Desktop / VS Code 左下角顯示）。
   - 如果目前在 `main`，先建立新分支再開始，命名格式：`feature/簡短描述`（例如 `feature/add-login`）。

2. **只有以下兩種情況才切回 `main`：**
   - 要開新分支之前（確保新分支從最新的 main 出發）
   - 要把已完成的 feature 分支合併回 main 時

3. 每完成一個獨立小功能，就開一個新分支，不要把不相關的修改混在同一個分支裡。

---

## Commit 規則

1. Commit message 一律使用**繁體中文**，格式：
   ```
   [動作] 簡短描述做了什麼
   ```
   範例：
   - `[新增] 建立 .gitignore 排除 .vs 資料夾`
   - `[修改] 調整河內塔遞迴邏輯`
   - `[修正] 修正檔名重複副檔名的問題`

2. 一個 commit 只做一件事，不要把「改功能 A」跟「改功能 B」寫在同一個 commit 裡。

3. Commit 前，先列出這次變更了哪些檔案、做了什麼，讓我確認過再送出，**不要自動執行 commit**。

---

## 動手前的溝通習慣

- 修改檔案前，先用一兩句話說明你打算怎麼做，等我確認後再實際動手（不要一次全部自動接受）。
- 如果不確定該進哪個分支、或該不該 merge，先問我，不要自己判斷。

---

## 其他

- 這是練習用 repo，允許放心測試、犯錯，不用過度謹慎，但**分支跟 commit 規則仍要遵守**，目的是練習正確流程。
